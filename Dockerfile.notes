# 2066 notes app — full-stack demo (auth, SQLite, sessions, revocation)
FROM python:3.12-slim

WORKDIR /app
COPY . /app
RUN pip install --no-cache-dir /app

WORKDIR /app/examples/notes_app
ENV HOST=0.0.0.0 PORT=8618
EXPOSE 8618
CMD ["python", "server.py"]
