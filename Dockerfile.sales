# 2066 Sales — the sales-machine vertical slice on the runtime
FROM python:3.12-slim

WORKDIR /app
COPY . /app
RUN pip install --no-cache-dir /app

WORKDIR /app/apps/sales
ENV HOST=0.0.0.0 PORT=8628
EXPOSE 8628
CMD ["python", "sales_server.py"]
