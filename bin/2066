#!/usr/bin/env python
"""Convenience wrapper so `bin/2066 run examples/hello.ai` matches roadmap §12."""

import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))

from runtime.cli import main  # noqa: E402

sys.exit(main(sys.argv[1:]))
